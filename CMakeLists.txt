cmake_minimum_required(VERSION 2.8)
project( StreetSignRecog )
find_package( OpenCV )
include_directories( ${OpenCV_INCLUDE_DIRS} )
add_executable( streetSignRecog classify_sign_template.cpp )
target_link_libraries( streetSignRecog ${OpenCV_LIBS} )
